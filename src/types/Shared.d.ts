export interface LoginReponse {
  accessToken: string
  refreshToken: string
  expires: string
}

export interface Config {
  username: string
  password: string
  accessToken: string
  refreshToken: string
  expires: string
}
